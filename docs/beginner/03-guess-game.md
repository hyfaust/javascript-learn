# 项目 03: 猜数字游戏 (Guess Number Game)

## 项目目标

创建一个有趣的猜数字游戏，系统随机生成 1-100 之间的数字，玩家通过不断猜测并根据提示（太大/太小）来逐步接近答案。通过这个项目，你将学习 JavaScript 中的随机数生成、条件判断、循环思维、数组操作和用户交互等核心概念。

## 核心知识点

1. **Math 对象**: `Math.random()`, `Math.floor()` 的使用
2. **条件判断**: `if-else`, `switch` 语句
3. **数组操作**: `push()`, `some()`, `forEach()` 方法
4. **DOM 操作**: 获取元素、修改内容、创建元素、操作 CSS 类
5. **事件处理**: 按钮点击、键盘事件（回车键提交）
6. **输入验证**: 检查空值、非数字、范围、重复猜测
7. **游戏状态管理**: 使用变量跟踪游戏进度
8. **CSS 动画**: `@keyframes` 定义动画效果

## 代码解析

### script.js 结构

```
script.js
├── 第 1 部分：变量声明与初始化    - 获取 DOM 元素，初始化游戏状态
├── 第 2 部分：生成随机数          - Math.random() 的使用
├── 第 3 部分：初始化游戏          - 重置所有状态
├── 第 4 部分：显示消息            - 根据不同状态显示不同样式的消息
├── 第 5 部分：更新尝试次数        - 实时显示猜测次数
├── 第 6 部分：添加猜测历史        - 记录每次猜测
├── 第 7 部分：更新历史显示        - 渲染历史记录列表
├── 第 8 部分：处理猜测            - 核心游戏逻辑和输入验证
├── 第 9 部分：事件监听器          - 按钮点击和键盘事件
└── 第 10 部分：页面加载初始化     - DOMContentLoaded 事件
```

### 关键概念解释

#### 随机数生成
```javascript
// 生成 1-100 之间的随机整数
Math.floor(Math.random() * 100) + 1
// Math.random() 返回 0-1 之间的数（不包含1）
// 乘以 100 后变成 0-100（不包含100）
// Math.floor() 向下取整，得到 0-99
// 加 1 后得到 1-100
```

#### 输入验证流程
```javascript
// 1. 检查是否为空
if (inputValue === '') { ... }

// 2. 检查是否为有效数字
if (isNaN(guess)) { ... }

// 3. 检查范围
if (guess < 1 || guess > 100) { ... }

// 4. 检查是否重复
if (guessHistory.some(item => item.guess === guess)) { ... }
```

#### switch 语句
```javascript
switch (item.result) {
  case 'low':
    // 处理猜小了的情况
    break;
  case 'high':
    // 处理猜大了的情况
    break;
  case 'correct':
    // 处理猜对的情况
    break;
}
```

## 运行方式

1. 直接用浏览器打开 `index.html` 文件
2. 可以使用 VS Code 的 Live Server 插件
3. 支持鼠标点击和键盘回车两种操作方式

### 操作说明
- 在输入框中输入 1-100 之间的数字
- 点击"猜！"按钮或按回车键提交
- 根据提示（太大/太小）调整下一次的猜测
- 猜中后点击"重新开始"按钮开始新游戏

## 常见错误

### 1. 输入非数字字符
- **现象**: 输入字母或符号
- **处理**: 代码会显示"请输入有效的数字！"并清空输入框

### 2. 输入超出范围
- **现象**: 输入 0 或 101 等超出 1-100 范围的数字
- **处理**: 代码会显示"请输入 1-100 之间的数字！"

### 3. 重复猜测
- **现象**: 输入之前已经猜过的数字
- **处理**: 代码会提示"你已经猜过 X 了，请换一个数字！"

### 4. 游戏结束后继续输入
- **现象**: 猜中后还想继续猜
- **处理**: 输入框和按钮会被禁用，必须点击"重新开始"

## 调试技巧

1. **查看目标数字**: 打开浏览器开发者工具 (F12)，在控制台中可以看到目标数字
2. **查看变量状态**: 在 `handleGuess` 函数中添加 `console.log()` 查看猜测过程
3. **修改范围**: 尝试修改 `getRandomNumber(1, 100)` 的参数来改变游戏难度
4. **测试边界**: 尝试输入 1、100、0、101 等边界值测试验证逻辑

## 挑战任务

### 简单
- [ ] 修改游戏范围为 1-50 或 1-200
- [ ] 在猜对时显示庆祝动画（如烟花效果）
- [ ] 添加一个"提示"按钮，给玩家一个范围提示

### 中等
- [ ] 实现难度选择：简单(1-50)、中等(1-100)、困难(1-500)
- [ ] 添加计时功能，记录玩家用了多长时间猜中
- [ ] 实现最佳记录功能：记录最少猜测次数（使用 `localStorage`）
- [ ] 添加音效：猜大/猜小/猜对时播放不同的声音

### 困难
- [ ] 实现"电脑猜玩家数字"的反向模式
  - 玩家想一个数字，电脑来猜
  - 玩家告诉电脑"太大了"或"太小了"
  - 电脑使用二分法策略来最快猜中
- [ ] 添加多玩家模式，记录每个玩家的成绩
- [ ] 实现一个排行榜系统，显示历史最佳成绩
- [ ] 添加"热/冷"提示：越接近目标数字，提示越"热"
